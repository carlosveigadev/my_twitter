<%= form_with(model: my_tweet, local: true) do |form| %>
  <% if my_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_tweet.errors.count, "error") %> prohibited this my_tweet from being saved:</h2>

      <ul>
      <% my_tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mytweet %>
    <%= form.text_area :mytweet %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
